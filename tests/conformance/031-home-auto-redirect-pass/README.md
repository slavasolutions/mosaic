# 031 - `/home → /` auto redirect

A clean site with `pages/index.md`. Per §3.2 and §3.6, engines MUST automatically add a
`/home → /` redirect to the route table. The expected routes include
`{ "url": "/home", "kind": "redirect" }`.
