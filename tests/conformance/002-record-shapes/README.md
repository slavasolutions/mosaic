# 002 - record shapes

Exercises every record-content combination (md only, json only, both) across direct files,
sidecar pairs, and folder-shape locations inside `collections/things/`. All shapes are valid
per SPEC §2.1 / §2.2. No diagnostics. The collection has no routing page so its records
have no URLs (they are addressable by ref only).
