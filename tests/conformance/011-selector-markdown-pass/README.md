# 011 - markdown heading selector resolves

`ref:about/company@our-mission` resolves the markdown-only record, then locates the
`## Our mission` heading (slug `our-mission`, per the §5.6 algorithm). No diagnostics.
