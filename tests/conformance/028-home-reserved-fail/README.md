# 028 - `pages/home.*` is reserved

`pages/home.md` exists alongside `pages/index.md`. The slug `home` at the top level of
`pages/` is reserved per §3.2 and TRUTHS #17. Engines MUST emit structural
`mosaic.home.reserved`.
